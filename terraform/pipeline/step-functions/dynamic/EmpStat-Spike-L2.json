{
  "Comment": "THROWAWAY ticket-2110 spike: runs candidate L2's _03_impute_spike_prototype.py (zero job role filled posts rows dropped) against real employment status clean data, instrumented with RunDiagnostics. Manual-start only - never wired into Ind-CQC-SLV. See ticket_2110_zero_jr.md. Delete once measured.",
  "StartAt": "Spike L2 impute (zero rows dropped)",
  "States": {
    "Spike L2 impute (zero rows dropped)": {
      "Type": "Task",
      "Resource": "arn:aws:states:::ecs:runTask.sync",
      "Parameters": {
        "Cluster": "${polars_cluster_arn}",
        "TaskDefinition": "${independent_cqc_task_arn}",
        "LaunchType": "FARGATE",
        "NetworkConfiguration": {
          "AwsvpcConfiguration": {
            "Subnets": ${public_subnet_ids},
            "SecurityGroups": ["${independent_cqc_security_group_id}"],
            "AssignPublicIp": "ENABLED"
          }
        },
        "Overrides": {
          "ContainerOverrides": [
            {
              "Name": "_03_independent_cqc-container",
              "Command": [
                "_02_employment_status/fargate/_03_impute_spike_prototype.py",
                "--cleaned_data_source", "${dataset_bucket_uri}/domain=03_ind_cqc/dataset=02_employment_status_02_clean/",
                "--spike_data_destination", "${dataset_bucket_uri}/domain=03_ind_cqc/dataset=02_employment_status_03_impute_spike_l2/",
                "--diagnostics_job_name", "empstat_spike_l2_red"
              ]
            }
          ]
        }
      },
      "End": true
    }
  }
}
